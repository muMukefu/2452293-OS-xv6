除了 getpid()，uptime() 也适合采用共享页优化。内核可以将系统时钟计数（ticks）存放在共享只读页中，并在时钟中断时更新该值，用户程序便可直接读取而无需陷入内核，从而减少系统调用开销。类似地，其他只读且变化较少的信息（如父进程 PID、用户 ID 等）也可以采用这种方式进行优化。而像 read()、write()、fork()、exec() 等需要执行内核操作或修改系统状态的系统调用，则不能通过共享页替代。